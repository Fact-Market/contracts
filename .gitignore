# Dependencies
node_modules/
.pnpm-store/
.yarn/
.yarn-integrity

# Build artifacts
build/
dist/
coverage/
*.tsbuildinfo

# Tact compiler output (regenerate via `pnpm build`)
*.fc
*.fif
*.boc
*.pkg
*.code.boc
*.ts.map

# Test output
.jest-cache/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Environment files — NEVER commit
.env
.env.*
!.env.example
*.env

# Secrets — NEVER commit
secrets/
secrets-*/
*.key
*.pem
*.p12
*.pfx
mnemonic*.txt
*mnemonic*
id_rsa*
id_ed25519*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Package manager locks other than pnpm (the one we use)
package-lock.json
yarn.lock

# Temporary / scratch
tmp/
scratch/
*.tmp
